//- index template 
extends layout.pug

block content
  article.grid-container.portfolio-intro
    div

      //- heading
      h1 Your company, my skills, now thats a match...

      //- portfolio description
      p.lead.text-light
        | Hi there. This site is a portfolio built using Express to show some of the work I have done in my time as a programmer.
        | Thank you for your time, and don't be a stranger.
  article.grid-container.portfolio-index

    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      //- project display generation using `each`
      each project in projects
        .cell
          a(href=`project/${project.id}`)
               img.thumbnail(src=`${project.image_url[0]}`)
               h5= project.project_name
